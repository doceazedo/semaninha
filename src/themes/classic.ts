import type { Theme } from '../interfaces';
import { Ratio } from '../enums';

const classic: Theme = {
  slug: 'classic',
  name: 'Clássico',
  ratios: [ Ratio.Vertical ],
  premium: false
}

export default classic;