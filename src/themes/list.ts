import type { Theme } from '../interfaces';
import { Ratio } from '../enums';

const list: Theme = {
  slug: 'list',
  name: 'Lista',
  ratios: [ Ratio.Vertical ],
  premium: false
}

export default list;