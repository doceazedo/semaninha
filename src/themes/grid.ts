import type { Theme } from '../interfaces';
import { Ratio } from '../enums';

const grid: Theme = {
  slug: 'grid',
  name: 'Mosaico',
  ratios: [ Ratio.Square ],
  premium: false
}

export default grid;